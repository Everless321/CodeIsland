# Task: Add SQLite-backed ServerStore

## Todos

- [x] Create `ServerStore.swift` with SQLite C API integration
- [x] Define `AuthType` enum and `ServerConfig` struct
- [x] Implement `configDir()` and `ensureConfigDir()`
- [x] Implement `loadServers()` — SELECT all ORDER BY name
- [x] Implement `saveServer(_:originalName:)` — DELETE old + INSERT OR REPLACE
- [x] Implement `deleteServer(name:)` — DELETE WHERE name = ?
- [x] Implement `sshCommand(servers:)` with jump host support
- [x] Implement `toResponseJSON(servers:)` for MCP responses

## Result

Created `/Users/everless/project/CodeIsland/Sources/CodeIsland/ServerStore.swift`.

- Uses raw `sqlite3` C API (zero SPM dependencies), reusing `~/.charon/charon.db`
- `ServerStore` is a caseless enum (stateless utility namespace)
- All DB operations open/close the connection per-call; no shared state
- SSH command generation mirrors Rust original: ProxyCommand for key-auth jump hosts, `-J` for password jump hosts
- `toResponseJSON` returns nested `jump_host` object when jump is resolved
- Logger uses `com.codeisland` subsystem, `ServerStore` category
